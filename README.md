# Moo UI

Moo UI is a standalone Bootstrap 5.3 component library with a compact,
shadcn-inspired visual language.

The source catalog is built with Python, Jinja, and libsass. Generated output
is disposable and is not committed.

The initial product slice contains the shared catalog shell and Button.
