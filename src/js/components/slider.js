// Slider component placeholder for Phase 2 build pipeline.
// Full implementation in Phase 5.

export default class MooSlider {
  constructor(element, config) {
    throw new Error("MooSlider is not yet implemented; Phase 5 will add the full native range input wrapper.");
  }

  static getInstance(element) {
    throw new Error("MooSlider is not yet implemented; Phase 5 will add the full native range input wrapper.");
  }

  static getOrCreateInstance(element, config) {
    throw new Error("MooSlider is not yet implemented; Phase 5 will add the full native range input wrapper.");
  }

  dispose() {
    throw new Error("MooSlider is not yet implemented; Phase 5 will add the full native range input wrapper.");
  }
}
