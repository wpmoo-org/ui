// Chart.js component placeholder for Phase 2 build pipeline.
// Full implementation in Phase 3.

export default class MooChart {
  constructor(element, config) {
    throw new Error("MooChart is not yet implemented; Phase 3 will add the full Chart.js wrapper.");
  }

  static getInstance(element) {
    throw new Error("MooChart is not yet implemented; Phase 3 will add the full Chart.js wrapper.");
  }

  static getOrCreateInstance(element, config) {
    throw new Error("MooChart is not yet implemented; Phase 3 will add the full Chart.js wrapper.");
  }

  dispose() {
    throw new Error("MooChart is not yet implemented; Phase 3 will add the full Chart.js wrapper.");
  }
}
