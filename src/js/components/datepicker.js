// Datepicker component placeholder for Phase 2 build pipeline.
// Full implementation in Phase 4.

export default class MooDatepicker {
  constructor(element, config) {
    throw new Error("MooDatepicker is not yet implemented; Phase 4 will add the full vanillajs-datepicker wrapper.");
  }

  static getInstance(element) {
    throw new Error("MooDatepicker is not yet implemented; Phase 4 will add the full vanillajs-datepicker wrapper.");
  }

  static getOrCreateInstance(element, config) {
    throw new Error("MooDatepicker is not yet implemented; Phase 4 will add the full vanillajs-datepicker wrapper.");
  }

  dispose() {
    throw new Error("MooDatepicker is not yet implemented; Phase 4 will add the full vanillajs-datepicker wrapper.");
  }
}

export class MooCalendar {
  constructor(element, config) {
    throw new Error("MooCalendar is not yet implemented; Phase 4 will add the full vanillajs-datepicker wrapper.");
  }

  static getInstance(element) {
    throw new Error("MooCalendar is not yet implemented; Phase 4 will add the full vanillajs-datepicker wrapper.");
  }

  static getOrCreateInstance(element, config) {
    throw new Error("MooCalendar is not yet implemented; Phase 4 will add the full vanillajs-datepicker wrapper.");
  }

  dispose() {
    throw new Error("MooCalendar is not yet implemented; Phase 4 will add the full vanillajs-datepicker wrapper.");
  }
}

export class MooDateRangePicker {
  constructor(element, config) {
    throw new Error("MooDateRangePicker is not yet implemented; Phase 4 will add the full vanillajs-datepicker wrapper.");
  }

  static getInstance(element) {
    throw new Error("MooDateRangePicker is not yet implemented; Phase 4 will add the full vanillajs-datepicker wrapper.");
  }

  static getOrCreateInstance(element, config) {
    throw new Error("MooDateRangePicker is not yet implemented; Phase 4 will add the full vanillajs-datepicker wrapper.");
  }

  dispose() {
    throw new Error("MooDateRangePicker is not yet implemented; Phase 4 will add the full vanillajs-datepicker wrapper.");
  }
}
