# @moo/ui

This package reserves the shorter Moo UI scope and points to the canonical
package:

```bash
npm install @wpmoo/ui
```

Use `@wpmoo/ui` in documentation, CDN examples, and production installs.
